/claude-fix 🛠️ **请立即根据以下审查意见修改代码：** \n\n> 以下是第 1 轮审查未通过的意见：\n\n 本轮审查结论为「请求修改」。四个需求（次日入口、突破按钮实时刷新、创角流程重排、境界徽章去冗余）均有对应改动，DOM/逻辑测试共 162 项全部通过，符号与 CSS class 引用也都齐全，方向正确。但存在一处实质性的逻辑缺陷需要处理：

【严重 - 逻辑缺陷 / 平衡破坏】`restToNextDay`（player.js:341）可被无限刷取，直接击穿每日活力上限这一核心进度门。
- 同文件的 `rolloverVitality`（player.js:322）用 `if (player.lastVitalityDate !== today)` 做了「每日仅回满一次」的守卫；而新增的 `restToNextDay` 没有任何守卫，无条件 `vitality = maxVitality` 并把 `lastVitalityDate` 重置为当天。
- 触发条件 `isVitalityDepleted()`（vitality < min(4,10,6)=4）并不能阻止滥用：玩家完全可以「把活力花到 <4 → 点『进入次日』回满 → 继续花 → 再回满」无限循环，等于当日可获得无限次修炼/探索/炼制，每日活力限制彻底失效，突破节奏被瞬间掏空。这与作者自己写的 `rolloverVitality` 守卫自相矛盾。
- 同时按钮文案是「🌌 进入次日」、日志写「新的一日到来」，但函数并不推进任何「日期」（`lastSeen` 不动、无天数计数），既名不副实，又是可被反复触发的根因。
- 建议修复（任选其一并补单测）：① 让「进入次日」真正推进游戏日（例如真的跨到下一天、只允许在当日剩余活力为 0 时触发一次，触发后 `lastVitalityDate` 落到「明天」键使其当日不可再用）；② 至少加「每日仅一次」的守卫，与 `rolloverVitality` 对齐；③ 若刻意做成免费回满，则应改掉「进入次日/新的一日」的措辞以免误导，并重新评估是否还需要每日活力这套机制。

【轻微】`isVitalityDepleted` 判定与横幅文案「今日行动力已尽」略偏绝对：活力 <4 时玩家仍可进行不耗活力的操作（坊市买卖、装备穿脱、扩容储物袋）且挂机被动修炼仍在进行，并非真正「卡死」。不影响功能，建议文案弱化为「活力不足，可闭关静候次日」之类。

【轻微/确认】`tick()`（app.js:1264）在修炼页每秒调用两次 `canBreakthrough`，但该函数只读 `ascended/nextTarget/isXpFull`，开销可忽略，且仅在「不可突破→可突破」跳变时重建一次面板（下一拍 `couldBreak` 已为 true，不会反复重建），无性能问题、无死循环，实现 OK。

【建议】`realmDisplay`（app.js）对 `major==='凡人'/'飞升'` 直接返回会忽略 sub，请确认这两个大境界确实只有单一小层；其余境界 `replace(/期$/,'')` 仅剥尾部「期」，逻辑安全。

CI/CD 与 workflows 本次未被改动（符合红线规则），无需人工处理 .github 配置。请优先修复 `restToNextDay` 的无限刷活力问题后再次提交。
