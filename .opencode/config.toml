# .opencode/config.toml

# 指定使用的底层大模型，例如 claude-3-5-sonnet
model = "glm-5.2"

# 开启自动应用代码修改（无需人工确认 yes）
auto_apply = true

# 加载 oh-my-opencode 插件
[[plugins]]
name = "oh-my-opencode"
# 如果 omo 有特定的配置项，可以写在这里，例如：
[plugins.oh_my_opencode_config]
enable_auto_lint = true  # 假设插件支持修改后自动格式化